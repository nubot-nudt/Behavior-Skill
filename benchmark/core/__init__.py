"""Evaluation core domain: evaluation loop (evaluator), BDDL goal construction
(skill_bddl), evaluation config loading (suite), observation preprocessing and
env config generation (observation).

Imported explicitly by skill_eval.py (CLI orchestration) and
benchmark/__init__.py (public API); this package does no re-exports, keeping
import paths minimal.
"""
